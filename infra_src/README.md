# infra_src

terraform login
terraform workspace list
terraform workspace select reorg
terraform init
terraform plan
terraform apply

## Requirements
Needs to create this:
- A proper VPC 
- An ECR
- A S3 for lambda
- A lambda function
